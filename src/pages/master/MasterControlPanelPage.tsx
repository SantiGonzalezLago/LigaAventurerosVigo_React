import { usePageTitle } from '../../hooks/usePageTitle';

export function MasterControlPanelPage() {
  usePageTitle('Gestión de partidas');

  return (
    <div className="page-content">
      <div className="card">
        <p>Panel de Master en construcción.</p>
      </div>
    </div>
  );
}
